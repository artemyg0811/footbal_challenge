import React from "react";
import { RNCameraViewProps } from "./CameraView.types";

export class CameraView extends React.PureComponent<RNCameraViewProps> {
    public start = () => {
        console.warn('[ error ] CameraView.start() not implemented')
    }

    public stop = () => {
        console.warn('[ error ] CameraView.stop() not implemented')
    }

    render() {
        return null;
    }
};